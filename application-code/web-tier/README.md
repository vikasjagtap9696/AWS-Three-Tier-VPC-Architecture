# Web Tier

React frontend for the student management application.

## Install and Build

```bash
cd application-code/web-tier
npm install
npm run build
```

For local development, run `npm start` after installing packages. In production, upload only the generated `build` folder to the Nginx web server.

